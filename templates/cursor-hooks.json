{
  "version": 1,
  "hooks": {
    "stop": [
      {
        "command": "~/.cc-notify/bin/smart-notify.sh --source cursor --event stop --kind terminal --title '✅ Cursor 已停下' --body '请查看当前会话结果' --priority low"
      }
    ]
  }
}
